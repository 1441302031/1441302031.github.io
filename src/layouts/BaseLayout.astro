---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
}

const siteName = 'Abner.DevLog';
const {
  title,
  description = 'Abner.DevLog，记录 Unity、小游戏性能优化、工程工具链与 AI 辅助开发实践。',
} = Astro.props;
const pageTitle = title === siteName ? title : `${title} | ${siteName}`;
const currentPath = Astro.url.pathname;
const navItems = [
  { href: '/', label: '首页' },
  { href: '/blog/', label: '博客' },
  { href: '/about/', label: '关于' },
  { href: 'https://github.com/1441302031', label: 'GitHub', external: true },
];
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="generator" content={Astro.generator} />
    <title>{pageTitle}</title>
    <link rel="icon" href="/favicon.svg" />
  </head>
  <body>
    <div class="scanline" aria-hidden="true"></div>
    <header class="site-header">
      <a class="brand" href="/" aria-label="返回首页">
        <span class="brand-mark" aria-hidden="true">A</span>
        <span class="brand-copy">
          <strong>{siteName}</strong>
          <small>Game Engineering Notes</small>
        </span>
      </a>
      <nav class="site-nav" aria-label="主导航">
        {
          navItems.map((item) => {
            const isActive =
              !item.external &&
              (item.href === '/'
                ? currentPath === '/'
                : currentPath === item.href || currentPath.startsWith(item.href));
            return (
              <a
                class:list={['nav-link', { active: isActive }]}
                href={item.href}
                target={item.external ? '_blank' : undefined}
                rel={item.external ? 'noreferrer' : undefined}
              >
                {item.label}
              </a>
            );
          })
        }
      </nav>
    </header>
    <main>
      <slot />
    </main>
    <footer class="site-footer">
      <p>© {new Date().getFullYear()} Abner.DevLog / Unity / MiniGame / Performance / Tools</p>
    </footer>
  </body>
</html>
